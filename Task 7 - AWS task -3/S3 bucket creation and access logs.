Steps followed For First Sub-task

1. Created 2 General Buckets.
  - In one bucket stored/ uploaded general data from PC.
  - Second bucket, set as to store logs for first bucket,
    now all access logs are stored in the second bucket.

2. Created lambda function to transfer logs to cloudwatch to see logs over cloudwatch.

3. Created log group and log stram in Cloud watch where we got all logs.
